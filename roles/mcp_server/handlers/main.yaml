# main.yml
# Handler to run at end of an Ansible playbook play, restarts MCP server.

---

- name: Restart mcp-server-remote
  ansible.builtin.systemd_service:
    name: mcp-server-remote
    state: restarted
    daemon_reload: true

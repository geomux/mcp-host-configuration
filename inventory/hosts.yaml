# hosts.yaml
# Defines which machines (hosts) to configure with the Ansible playbook.

mcp_hosts:
  hosts:
    box1:
      ansible_host: 203.0.113.10
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/mykey.pem
